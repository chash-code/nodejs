// Import the checkPrime function from math.js
import { checkPrime } from './math.js';

// Test the function with different values
console.log(checkPrime(7));    // true - 7 is prime
console.log(checkPrime(10));   // false - 10 is not prime
console.log(checkPrime(13));   // true - 13 is prime
console.log(checkPrime(1));    // false - 1 is not prime
console.log(checkPrime(29));   // true - 29 is prime
